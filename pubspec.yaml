name: gesturely
description: "Learn ASL with confidence"
publish_to: "none" # Prevent publishing to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

# Dependencies your app needs
dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.0.0
  cupertino_icons: ^1.0.8
  video_player: ^2.5.0   # <-- Added video_player package

# Development dependencies
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# Flutter-specific configuration
flutter:
  uses-material-design: true

  # Assets section (uncomment and add files if needed)
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # Fonts section (uncomment and add fonts if needed)
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
